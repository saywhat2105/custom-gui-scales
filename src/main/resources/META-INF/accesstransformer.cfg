# In 1.20.1 OptionInstance.ValueSet is package-private, but it is the only way to put a custom
# widget (our "Custom GUI Scales..." button) into an OptionsList row. Make the interface public
# so SettingsIntegration can implement it. Forge and NeoForge (1.20.1) both apply this at runtime.
public net.minecraft.client.OptionInstance$ValueSet
